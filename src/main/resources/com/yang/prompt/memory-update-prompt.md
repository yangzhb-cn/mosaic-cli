# 长期记忆更新任务

你负责更新 MosaicCoder 的长期记忆文件 `workspace/Mosaic.md`。

只输出完整的新 `Mosaic.md` 内容，不要输出解释，不要使用 Markdown 代码块。

只保留长期有效的信息：
- 稳定的用户偏好
- 反复出现的工作方式
- 之后仍可能有用的项目事实
- 用户明确要求长期记住的重要约束

删除短期任务细节、临时计划、日志、命令输出、一次性调试上下文。
尽量保留现有 `Mosaic.md` 中有用的结构和长期有效内容。
